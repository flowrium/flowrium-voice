# Apple Benchmarks

这里记录 `whisper.cpp` 在 macOS / Apple Silicon 开发机上的参考值。

```bash
bash verification-3-edge/whisper.cpp/benchmarks/apple/run_macos_benchmark.sh
```

输出位于 `benchmarks/apple/results/`：

- `<label>.log`
- `<label>.time.txt`
- `<label>-benchmark.json`
- `<label>-benchmark.md`
